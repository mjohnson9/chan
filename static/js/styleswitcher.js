var stylesheet_selectors = {};function setActiveStyleSheet(title) {	var current_sheet = getActiveStyleSheet();	if(stylesheet_selectors[current_sheet]) stylesheet_selectors[current_sheet].removeClass("active");	var i, a, main;	for(i=0; (a = document.getElementsByTagName("link")[i]); i++) {		if(a.getAttribute("rel").indexOf("style") != -1 && a.getAttribute("title")) {			a.disabled = true;			if(a.getAttribute("title") == title) a.disabled = false;		}	}	if(stylesheet_selectors[title]) stylesheet_selectors[title].addClass("active");}function getActiveStyleSheet() {	var i, a;	for(i=0; (a = document.getElementsByTagName("link")[i]); i++) {		if(a.getAttribute("rel").indexOf("style") != -1 && a.getAttribute("title") && !a.disabled) return a.getAttribute("title");	}	return null;}function getPreferredStyleSheet() {	var i, a;	for(i=0; (a = document.getElementsByTagName("link")[i]); i++) {		if(a.getAttribute("rel").indexOf("style") != -1			&& a.getAttribute("rel").indexOf("alt") == -1			&& a.getAttribute("title")			) return a.getAttribute("title");	}	return null;}function createCookie(name,value,days) {	if (days) {		var date = new Date();		date.setTime(date.getTime()+(days*24*60*60*1000));		var expires = "; expires="+date.toGMTString();	}	else expires = "";	document.cookie = name+"="+value+expires+"; path=/";}function readCookie(name) {	var nameEQ = name + "=";	var ca = document.cookie.split(';');	for(var i=0;i < ca.length;i++) {		var c = ca[i];		while (c.charAt(0)==' ') c = c.substring(1,c.length);		if (c.indexOf(nameEQ) == 0) return c.substring(nameEQ.length,c.length);	}	return null;}var item_clicked = function(ev) {	var us = $(ev.currentTarget);	setActiveStyleSheet(us.data("theme-name"));}$(function() {	var cookie = readCookie("style");	var title = cookie ? cookie : getPreferredStyleSheet();	setActiveStyleSheet(title);	var theme_items = $("#themeselector-menu").children();	for(var i = 0; i < theme_items.length; i++) {		var theme_item = $(theme_items[i]);		theme_item.click(item_clicked);		stylesheet_selectors[theme_item.data("theme-name")] = theme_item;	}	var current_sheet = getActiveStyleSheet();	if(stylesheet_selectors[current_sheet]) stylesheet_selectors[current_sheet].addClass("active");});$(window).unload(function() {	var title = getActiveStyleSheet();	createCookie("style", title, 365);});var cookie = readCookie("style");var title = cookie ? cookie : getPreferredStyleSheet();setActiveStyleSheet(title);